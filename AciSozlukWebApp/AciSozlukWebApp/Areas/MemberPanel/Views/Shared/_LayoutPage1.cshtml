@using AciSozlukWebApp.Models;
@{
    Member m = new Member();
    if (Session["MemberSession"] != null)
    {
        m = Session["MemberSession"] as Member;
    }
}


<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
</head>
<body>
    <a href="#" style="color:black;text-decoration:none"><strong>Rolüm =></strong> @m.MemberRole.Name</a>&nbsp;
    <a href="@Url.Action("Logout", "HomePageMember", new { area = "" })">Çıkış</a>
    <div>
        @RenderBody()
    </div>
</body>
</html>
